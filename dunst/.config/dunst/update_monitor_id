#!/usr/bin/env bash

declare -r CONFIG_FILE="$XDG_CONFIG_HOME/dunst/dunstrc"
OUTPUT=$(mktemp)
declare -r OUTPUT
trap 'rm -f $OUTPUT' EXIT

sed 's/follow[[:space:]]*=[[:space:]]*[a-z]\+/follow = none/' "$CONFIG_FILE" >"$OUTPUT"

dunstctl reload "$OUTPUT"
