#!/bin/bash

# Synchronize mail directory
polybar-msg action "#mail.hook.1"
mailsync
polybar-msg action "#mail.hook.0"

# Open neomutt if new mails exist
NEW_MAIL=$(/home/yasser/.config/polybar/mail)

echo $NEW_MAIL

if [[ $NEW_MAIL != "" ]]; then
    alacritty -e neomutt
fi
