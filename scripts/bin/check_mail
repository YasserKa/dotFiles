#!/bin/bash

# Synchronize mail directory
polybar-msg hook mail 2
mailsync
polybar-msg hook mail 1

# Open neomutt if new mails exist
NEW_MAIL=$(/home/yasser/.config/polybar/mail)

echo $NEW_MAIL

if [[ $NEW_MAIL != "" ]]; then
    alacritty -e neomutt
fi
